{
  "name": "barcode-scanner",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy public",
    "dev": "wrangler pages dev public",
    "dev:local": "wrangler pages dev public --local"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}